---
description: Quick 720p preview render of the current project.
argument-hint: <project-name>
---

# /preview — fast 720p render

Alias for `/render $1 --preview`. Use to sanity-check the edit before
the expensive final render.

See `/render` for the full behavior.
